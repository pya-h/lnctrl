# Disable source-map processing of node_modules. This silences the noisy
# "Failed to parse source map" warnings emitted by source-map-loader for
# third-party packages that ship broken/missing .map files (e.g. plotly.js
# referencing maplibre-gl-unminified.js.map). Also trims the production build.
GENERATE_SOURCEMAP=false
